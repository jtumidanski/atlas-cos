package com.atlas.cos.processor;

import java.util.Optional;

import com.atlas.cos.model.Party;

public final class PartyProcessor {
   private PartyProcessor() {
   }

   public static Optional<Party> getPartyForCharacter(int characterId) {
      return Optional.empty();
   }
}
